# TaskFlow API

Dieses Projekt dient als Beispiel für die Implementierung einer  Task-Management-API in PHP. Sie wird im laufe eines Tutorials erstellt, um die Grundlagen von Design-Patterns und Best Practices in der Softwareentwicklung zu vermitteln.

## How to run

```bash
php -S localhost:8000 -t public
```

Dann im Browser `http://localhost:8000` aufrufen.
